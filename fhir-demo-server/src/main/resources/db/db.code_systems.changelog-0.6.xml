<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

	<property name="json" value="JSONB" dbms="postgresql" />
	<property name="json" value="varchar(5000)" dbms="h2" />

	<changeSet author="hhund" id="db.code_systems.changelog-0.6">
		<createView viewName="current_code_systems" replaceIfExists="true">
			SELECT DISTINCT ON (code_system_id) code_system_id, version, code_system
			FROM code_systems WHERE NOT deleted ORDER BY code_system_id, version DESC
		</createView>
		
		<sql>
			ALTER TABLE current_code_systems OWNER TO ${liquibase_user};
			GRANT ALL ON TABLE current_code_systems TO ${liquibase_user};
			GRANT SELECT ON TABLE current_code_systems TO ${server_users_group};
		</sql>
	</changeSet>
</databaseChangeLog>