<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

	<property name="json" value="JSONB" dbms="postgresql" />
	<property name="json" value="varchar(5000)" dbms="h2" />

	<changeSet author="hhund" id="db.structure_definitions.changelog-0.6">
		<createView viewName="current_structure_definitions" replaceIfExists="true">
			SELECT DISTINCT ON (structure_definition_id) structure_definition_id, version, structure_definition
			FROM structure_definitions WHERE NOT deleted ORDER BY structure_definition_id, version DESC
		</createView>
		
		<sql>
			ALTER TABLE current_structure_definitions OWNER TO ${liquibase_user};
			GRANT ALL ON TABLE current_structure_definitions TO ${liquibase_user};
			GRANT SELECT ON TABLE current_structure_definitions TO ${server_users_group};
		</sql>
	</changeSet>
</databaseChangeLog>