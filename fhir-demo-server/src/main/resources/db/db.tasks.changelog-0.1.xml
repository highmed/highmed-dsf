<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

	<property name="json" value="JSONB" dbms="postgresql" />
	<property name="json" value="varchar(5000)" dbms="h2" />

	<changeSet author="hhund" id="db.tasks.changelog-0.1">
		<createTable tableName="tasks">
			<column name="task_id" type="UUID">
				<constraints nullable="false" />
			</column>
			<column name="version" type="BIGINT">
				<constraints nullable="false" />
			</column>
			<column name="deleted" type="BOOLEAN">
				<constraints nullable="true" />
			</column>
			<column name="task" type="${json}">
				<constraints nullable="false" />
			</column>
		</createTable>

		<addPrimaryKey tableName="tasks" columnNames="task_id, version" />

		<sql dbms="postgresql">
			ALTER TABLE tasks OWNER TO ${liquibase_user};
			GRANT ALL ON TABLE tasks TO ${liquibase_user};
			GRANT SELECT, INSERT, UPDATE ON TABLE tasks TO ${server_users_group};
			CREATE INDEX tasks_task_id_index ON tasks USING btree (task_id);
			CREATE INDEX tasks_task_index ON tasks USING gin (task);
			CREATE INDEX tasks_task_id_version_index ON tasks USING btree (task_id, version);
		</sql>
	</changeSet>
</databaseChangeLog>