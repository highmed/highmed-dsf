<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

	<property name="json" value="JSONB" dbms="postgresql" />
	<property name="json" value="varchar(5000)" dbms="h2" />

	<changeSet author="hhund" id="db.subscriptions.changelog-0.1">
		<createTable tableName="subscriptions">
			<column name="subscription_id" type="UUID">
				<constraints nullable="false" />
			</column>
			<column name="version" type="BIGINT">
				<constraints nullable="false" />
			</column>
			<column name="deleted" type="BOOLEAN">
				<constraints nullable="true" />
			</column>
			<column name="subscription" type="${json}">
				<constraints nullable="false" />
			</column>
		</createTable>

		<addPrimaryKey tableName="subscriptions" columnNames="subscription_id, version" />

		<sql dbms="postgresql">
			ALTER TABLE subscriptions OWNER TO ${liquibase_user};
			GRANT ALL ON TABLE subscriptions TO ${liquibase_user};
			GRANT SELECT, INSERT, UPDATE ON TABLE subscriptions TO ${server_users_group};
			CREATE INDEX subscriptions_subscription_id_index ON subscriptions USING btree (subscription_id);
			CREATE INDEX subscriptions_subscription_index ON patients USING gin (patient);
			CREATE INDEX subscriptions_subscription_id_version_index ON subscriptions USING btree (subscription_id, version);
		</sql>
	</changeSet>
</databaseChangeLog>