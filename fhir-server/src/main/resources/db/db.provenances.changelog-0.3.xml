<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

	<property name="json" value="JSONB" dbms="postgresql" />
	<property name="json" value="varchar(5000)" dbms="h2" />

	<changeSet author="hhund" id="db.provenances.changelog-0.3">
		<sql dbms="postgresql">
			DROP INDEX provenances_provenance_id_index;
			DROP INDEX provenances_provenance_index;
			DROP INDEX provenances_provenance_id_version_index;
			CREATE INDEX provenance_id_index ON provenances USING btree (provenance_id);
			CREATE INDEX provenance_index ON provenances USING gin (provenance);
			CREATE INDEX provenance_id_version_index ON provenances USING btree (provenance_id, version);
		</sql>
	</changeSet>
</databaseChangeLog>