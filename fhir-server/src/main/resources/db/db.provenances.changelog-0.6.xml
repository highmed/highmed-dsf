<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

	<property name="json" value="JSONB" dbms="postgresql" />
	<property name="json" value="varchar(5000)" dbms="h2" />

	<changeSet author="hhund" id="db.provenances.changelog-0.6">
		<createView viewName="current_provenances" replaceIfExists="true">
			SELECT DISTINCT ON (provenance_id) provenance_id, version, provenance
			FROM provenances WHERE NOT deleted ORDER BY provenance_id, version DESC
		</createView>
		
		<sql>
			ALTER TABLE current_provenances OWNER TO ${liquibase_user};
			GRANT ALL ON TABLE current_provenances TO ${liquibase_user};
			GRANT SELECT ON TABLE current_provenances TO ${server_users_group};
		</sql>
	</changeSet>
</databaseChangeLog>